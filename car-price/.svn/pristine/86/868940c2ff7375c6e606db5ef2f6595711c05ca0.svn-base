require.config({
    baseUrl: url + '/controller',
    urlArgs: 'v=' + version,
    paths: {
        'static': '../static',
        'template': '../template',
        'widget': '../widget'
    }
});

require(['home']);
